SASS II

1. Uso de "map" y "map-get"
	Se agrupó la paleta de colores en una variable mapa, y se fueron recuperando los valores con el comando "map-get" según la necesidad de cada estilo.

2. Uso de "extend"
	Se utilizó el extend para llamar los estilos de la clase .subtituloDePagina (main.scss) en la clase .footer__titulo (header&footer.scss, linea 85). 

3. Uso de "mixin"
	Se creó un mixin que contiene las propiedades de flexbox, y que son intercambiables a través de parámetros. Se usó el mixin en todas las secciones que incluyen flexbox para configurar el layout.


SEO

1. Optimización de imágenes
	Se comprimieron las imágenes uitlizadas en el sitio a través de la página compressor.io.

2. Keywords
	Se agregaron diferentes palabras clave en las páginas del sitio, en base a una investigación realizada en Google, Google Trends, Keyword Tool y Answer the public.

3. Descripción
	Se agregaron las "meta description" en todas las páginas del sitio, contextual de acuerdo a su contenido. 

4. Favicon
	Se creó y se agregó una imagen para el "favicon" en todas las páginas.

5. Encabezados
	Se revisaron los encabezados de todas las páginas para que fueran coherentes jerárquicamente entre sí y con el contenido.
	

